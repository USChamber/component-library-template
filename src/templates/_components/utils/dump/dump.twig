{% if data is not defined %}
    {% set data = contextData|default(_context) %}
{% endif %}
<pre class="block py-1 px-3 mt-3 bg-code whitespace-pre-wrap">{{ data|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
